# TrajOpt 项目模块化结构

## 新增模块

### 1. `convexify.py` - 凸化模块
**功能**: 将非凸问题凸化为QP子问题

**核心函数**:
- `convexify_objective()`: 将目标函数（路径长度+松弛变量惩罚）二次化
- `convexify_collision_constraints()`: 将碰撞约束线性化为不等式约束
- `add_endpoint_constraints()`: 添加起点和终点固定的等式约束
- `build_qp_subproblem()`: 构建完整的QP子问题（统一接口）

**调用方式**:
```python
from Core_Trajopt.convexify import build_qp_subproblem

H, c, A_eq, b_eq, A_ineq, b_ineq, M = build_qp_subproblem(
    x_curr, T, N, D_SAFE, mu
)
```

## 模块化优势

### 代码简化
- **原来**: `sequential_convex_opt.py` 400+ 行，包含大量QP构建代码
- **现在**: 主文件减少到 ~300 行，凸化逻辑独立到 `convexify.py`

### 职责清晰
- `sequential_convex_opt.py`: SCO主循环（惩罚迭代、凸化迭代、信赖域迭代）
- `convexify.py`: QP子问题构建（目标函数、约束凸化）
- `qp_solver_interface.py`: QP求解器封装
- `trust_region.py`: Merit函数和信赖域更新

### 易于维护
- 修改凸化策略：只需修改 `convexify.py`
- 添加新约束：在 `convexify.py` 中添加新函数
- 更换QP求解器：只需修改 `qp_solver_interface.py`

## 使用示例

```python
# 导入模块化的凸化函数
from Core_Trajopt.convexify import build_qp_subproblem

# 在SCO循环中使用
for conv_iter in range(MAX_ITER_CONVEXIFY):
    # 一行代码构建完整QP子问题
    H, c, A_eq, b_eq, A_ineq, b_ineq, M = build_qp_subproblem(
        x_curr, T, N, D_SAFE, mu
    )
    
    # 求解QP
    delta_X, success = solve_qp(H, c, s, A_eq, b_eq, A_ineq, b_ineq, num_state=T*N)
```

## 测试结果

✅ 模块化后代码功能完全正常
✅ 优化效果与原来一致
✅ 代码可读性和可维护性显著提升
